<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div class="w-full h-[90vh] flex flex-col items-center justify-center">
      <div
        class="w-fit h-fit flex flex-col items-center justify-center gap-6 p-12 text-center"
      >
        <div>
          <h1
            class="cursor-default text-5xl leading-[4rem] font-extrabold uppercase text-neutral-100"
          >
            500 erro interno do servidor
          </h1>
          <p
            class="cursor-default text-[1.375rem] leading-7 font-normal text-neutral-200"
          >
            <%= errorMessage %>
          </p>
        </div>
        <div
          class="w-fit h-fit flex flex-col items-center justify-center gap-3 bg-neutral-900 bg-opacity-20 backdrop-blur-xl rounded-3xl p-12 border-2 border-solid border-neutral-800"
        >
          <% if (errorDetails) { %>
          <code class="text-neutral-100 font-medium text-left">
            <pre><%= errorDetails %></pre>
          </code>
          <% } %>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
